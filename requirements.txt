# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ── ML / model inference ──────────────────────────────────────────────────────
torch>=2.0.0
transformers>=4.35.0,<5.0.0
sentencepiece>=0.1.99
sacremoses
numpy>=1.24.0

# ── IndicTrans2 preprocessing toolkit ────────────────────────────────────────
# Installed from GitHub — handles preprocess_batch / postprocess_batch
git+https://github.com/VarunGumma/IndicTransToolkit.git

# ── Indic NLP (required by IndicTransToolkit) ─────────────────────────────────
indic-nlp-library
